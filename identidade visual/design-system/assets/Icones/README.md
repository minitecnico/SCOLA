Ícones do sistema com os nomes exatos de `public/`. `favicon.svg`: símbolo com cantos de 22%, fundo transparente fora do quadrado. `icon-192.png`, `icon-512.png` e `apple-touch-icon.png` (180 × 180): quadrado `amarelo` sólido de ponta a ponta, sem transparência e sem cantos arredondados, com o S `preto` no centro (42% do lado no 512, dentro do círculo seguro de 80%). O `favicon.ico` (16, 32 e 48 px) vai no pacote para download, pois não é um tipo que esta biblioteca exibe.
